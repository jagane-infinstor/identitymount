name: identitymount

docker_env:
  image:  identitymount

entry_points:
  main:
    parameters:
        input_data_spec: {type: str}
    command: "python xformcode --input_data_spec {input_data_spec}"
